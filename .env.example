# URL of the backend API (no trailing slash).
# Local: http://localhost:5001
# Vercel: set VITE_API_URL to your Render URL in Project > Settings > Environment Variables.
VITE_API_URL=http://localhost:5001
