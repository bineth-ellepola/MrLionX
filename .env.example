# URL of the backend API (no trailing slash).
# Local dev: copy to .env.local (points at the server running on your PC).
# Production builds use .env.production (https://mrlionx.onrender.com).
VITE_API_URL=http://localhost:5001
